# Orchestrator — starts all engines:
#   docker compose up -d
# Per-engine (standalone):
#   docker compose -f compose.mongo.yaml up -d      # MongoDB only
#   docker compose -f compose.postgres.yaml up -d   # PostgreSQL only
include:
  - compose.mongo.yaml
  - compose.postgres.yaml
