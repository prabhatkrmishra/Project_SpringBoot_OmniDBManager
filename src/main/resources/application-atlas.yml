# MongoDB Atlas profile: run with -Dspring-boot.run.profiles=atlas (or --spring.profiles.active=atlas).
# Requires OVERRIDE_MONGODB_URI (Atlas cluster-admin connection string) in .env or the environment.
spring:
  mongodb:
    uri: ${OVERRIDE_MONGODB_URI:}
